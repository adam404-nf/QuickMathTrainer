# App

Application entry and page composition layer.

Responsibilities:

- Mount routes and the top-level layout.
- Keep global providers in one place.
- Compose feature modules into pages.

Do not put question generation, answer checking, or result calculation logic here.
