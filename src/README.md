# Frontend Source

This folder follows the frontend architecture described in `docs/07-frontend-architecture.md`.

The current skeleton only defines module boundaries. Functional React and TypeScript code should be added in later implementation steps.
