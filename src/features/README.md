# Features

Feature modules own product behavior.

Current modules:

- `practice`: Flashcard session flow.
- `questions`: Question engine, templates, generators, and constraints.
- `results`: Attempts, summaries, mistakes, and history.
- `settings`: User preferences and practice options.

Keep product-specific logic inside the closest feature module.
