# Practice Hooks

React hooks that are specific to the practice session live here.

Examples:

- Session state orchestration
- Keyboard submit behavior for practice
- Per-question timer wiring

Generic hooks should go to `shared/hooks`.
